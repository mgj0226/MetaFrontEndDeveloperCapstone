import Logo from '../images/Logo.svg';

const Footer = () => {
    return (
        <footer className='footer'>
            <img src={Logo} alt="logo" />
        </footer>
    );
};

export default Footer;